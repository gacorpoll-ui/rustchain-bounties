# Stub workflow — RTC reward disabled on fork
name: RTC Reward on PR Merge

on:
  pull_request_target:
    types: [closed]

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "RTC reward disabled on fork"