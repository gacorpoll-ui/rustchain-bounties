# Stub workflow — Auto-Award RTC disabled on fork
name: Auto-Award RTC for Merged PRs

on:
  pull_request:
    types: [closed]

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Auto-Award disabled on fork"