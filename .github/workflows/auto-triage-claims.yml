# Stub workflow — Auto Triage disabled on fork
# Original requires scripts/ directory not present on fork
name: Auto Triage Claims

on:
  workflow_dispatch:
  schedule:
    - cron: "17 * * * *"

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Auto Triage disabled on fork"