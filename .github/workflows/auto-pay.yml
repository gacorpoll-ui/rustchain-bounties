# Stub workflow — Auto-Pay disabled on fork
name: RTC Auto-Pay on PR Merge

on:
  pull_request_target:
    types: [closed]

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Auto-Pay disabled on fork"