# Stub workflow — Glassworm Protocol disabled on fork
name: The Glassworm Protocol

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Glassworm Protocol disabled on fork"