# Stub workflow — BCOS checks disabled on fork
# Original requires tier labels and external tools not available in fork
name: BCOS Checks

on:
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - run: echo "BCOS checks disabled on fork"